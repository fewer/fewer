# Repository

## What is it?

## What is returned from it?

## How do I create one?

```ts
import { createRepository } from 'fewer';

interface IUsers {
  id: number;
  firstName: string;
  lastName?: string;
}

const Users = createAIUsers>('users');
```
