import { createDatabase } from 'fewer';
import { <%= adapter %>Adapter as Adapter } from '@fewer/adapter-<%= adapter.toLowerCase() %>';

export default createDatabase({
    adapter: new Adapter({
        // TODO: Put your database connection here!
    })
});
